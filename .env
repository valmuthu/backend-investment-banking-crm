# Heroku Config Vars (set these in Heroku Dashboard or via CLI)
# DO NOT commit this file - these are just examples

NODE_ENV=production
PORT=5000
JWT_SECRET=your-super-secure-jwt-secret-key-at-least-32-characters-long-random-string
FRONTEND_URL=https://your-frontend-domain.netlify.app

# Optional MongoDB (if you want to add database later)
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/dbname

# Optional email settings (for future features)
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASS=your-app-password
